<nav class="navbar navbar-expand-lg navbar-dark p-2 pl-4 pr-4 bg-vcodes">
  <div class="container-fluid">
    <a
      class="navbar-brand tippy"
      data-tippy-content="Copy to Clipboard"
      data-copytext-text="<%- user_client.tag %>"
      onclick="copyText()"
      onmouseout="uncopyText()"
      id="copyText"
    >
      <img
        class="avatar"
        src="<%- user_client.avatarURL %>"
        alt=""
        width="30"
        height="24"
        class="d-inline-block align-text-top"
      />
      <%- user_client.tag %>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbar"
      aria-controls="navbar"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/dashboard"
            >Dashboard</a
          >
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <div class="dropdown-center dropstart">
            <a
              class="nav-link dropdown-toggle active"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              <%- user.username %>
            </a>
            <ul class="dropdown-menu dropdown-dark dropleft">
              <li><a class="dropdown-item" href="/logout">Logout</a></li>
            </ul>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
